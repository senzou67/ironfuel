name = "onefood"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = "."
exclude = ["node_modules", ".git", "netlify", ".claude", ".wrangler"]

# KV namespace for trial IP tracking
# Bind in Cloudflare dashboard: Settings > Functions > KV namespace bindings
# Variable name: TRIAL_KV

# Cron trigger for daily notifications (every 30 minutes)
# Set up via Cloudflare dashboard or use an external cron service
# calling POST /api/daily-notification with X-Admin-Key header
