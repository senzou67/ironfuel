{
  "name": "onefood",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && npx wrangler pages deploy public/ --project-name=onefood",
    "preview": "npm run build && npx wrangler pages dev public/"
  },
  "dependencies": {
    "stripe": "^20.4.1",
    "firebase-admin": "^12.0.0"
  }
}
