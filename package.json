{
  "name": "onefood",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "deploy": "npm run build && wrangler deploy",
    "preview": "npm run build && wrangler dev"
  },
  "dependencies": {
    "stripe": "^20.4.1",
    "firebase-admin": "^12.0.0"
  }
}
